<h1>Edit Merchant</h1>
<%# <%= binding.pry %> 
  <%= form_with model: @merchant, url: admin_merchant_path(@merchant), data: {turbo: false}, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "submit" %>
  <% end %>



<%# <%= form_with url: "/admin/merchants/#{@merchant.id}", method: :patch, data: {turbo: false} do |f| %>